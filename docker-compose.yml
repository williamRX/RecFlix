version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./data:/app/data
    environment:
      - ENVIRONMENT=development
    command: uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --reload
